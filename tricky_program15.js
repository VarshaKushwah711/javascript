console.log(0.1 + 0.2 === 0.3);

// false

// Explanation: Due to floating-point precision issues in JavaScript (and most other languages), 0.1 + 0.2 doesn't result in exactly 0.3. It results in something like 0.30000000000000004, which is not strictly equal to 0.3.